version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pwd
        - ls -la
        - ls -la frontend/ || echo "no frontend dir"
        - ls -la */ || echo "no subdirs"
    build:
      commands:
        - echo "debug build"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
